import * as uiIcons from '@material-ui/icons';
import React from 'react';

const Icons: React.FC = () => {
    const names: string[] = Object.keys(uiIcons);
    //{names.map(name => React.createElement(uiIcons[name]['type']))}
    return (
        <>

        </>
    );
}

export default Icons;